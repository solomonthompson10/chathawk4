---
import { markdownify } from "@lib/utils/textConverter";
import { Image } from "@astrojs/image/components";
const { banner } = Astro.props;
const { title, button, image, content } = banner;

---

<section class="section banner relative" id="vanta-animation">
  <div class="container your-element-selector">
    <div class="row items-center">
      <div class="lg:col-6">
        <h1 class="banner-title" set:html={markdownify(title)} />
        <p class="mt-6" set:html={markdownify(content)} />

        {
          button.enable && (
          <link href="https://assets.calendly.com/assets/external/widget.css" rel="stylesheet">
        <script src="https://assets.calendly.com/assets/external/widget.js" type="text/javascript" async></script>
            <a class="btn m-3 block sm:inline-block btn-primary" href="" onclick="
            Calendly.initPopupWidget({ url: 'https://calendly.com/chathawk' }); return false;
            ">
              {button.label}
            </a>
          )
        }
      </div>
      <div class="lg:col-6">
        <!-- <VantaAnimation/> -->

        <Image
          class="w-full object-contain"
          alt="banner"
          src={image}
          width={700}
          height={600}
        />
      </div>
    </div>
  </div>

  <!-- <Image
    class="banner-shape absolute -top-28 right-0 -z-[1] w-full max-w-[30%]"
    alt="banner"
    src="/images/banner-shape.png"
    width={496}
    height={466}
  /> -->
</section>
